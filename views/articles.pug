doctype=html
html
    head
        title Contact Information
        style
            include ../public/css/styles.css
    body
        h1 Articles
        div(class="wrapper")
            each article in articles
                if article.isActive === true
                    div(class="article")
                        h2= article.title
                        div(class="article-body")!= article.content
                        a(href="/article/" + article.id) Read more...
                else
                    p(class="error") Not active article here